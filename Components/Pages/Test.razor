@page "/test"
@rendermode InteractiveServer
@using BlazorTest.Components

<h3>Test Chart</h3>
<ChartComponent CanvasId="bankRevenueChart"
                Labels="@labels"
                Values="@values"
                Title="Test Revenue" />

@code {
    private string[] labels = new[] { "A", "B", "C", "D", "E", "F", "G" };
    private decimal[] values = new decimal[] { 100, 200, 300, 400, 500, 600, 700 };
}